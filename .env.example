QUICKBOOKS_CLIENT_ID=your_client_id_here
QUICKBOOKS_CLIENT_SECRET=your_client_secret_here
QUICKBOOKS_REALM_ID=your_company_id_here
QUICKBOOKS_REDIRECT_URI=http://localhost:8000/callback
QUICKBOOKS_ENVIRONMENT=sandbox

# --- Refresh token (encrypted at rest) ---
# The OAuth handshake writes the refresh token here for you as
# QUICKBOOKS_REFRESH_TOKEN_ENC — an AES-256-GCM blob, not a value you paste by
# hand. Intuit's app security requirements call for the refresh token to be
# encrypted with a symmetric algorithm and for the key to live in a separate
# configuration file, so the AES key is written to a sibling `.qbo-token-key`
# file (mode 0600) next to this one. Back up and restrict BOTH together: the
# blob is useless without the key, and the key is useless without the blob.
# QUICKBOOKS_REFRESH_TOKEN_ENC=<written automatically by the OAuth handshake>
#
# Legacy: a plaintext QUICKBOOKS_REFRESH_TOKEN is still read if it is the only
# thing present — on the next start the server encrypts it, writes
# QUICKBOOKS_REFRESH_TOKEN_ENC, and deletes the plaintext line. Set it only to
# seed a token you already hold from a previous install.
# QUICKBOOKS_REFRESH_TOKEN=your_refresh_token_here

# Optional: QUICKBOOKS_TOKEN_STORE_PATH — absolute path used BOTH to read config
# at startup and to persist rotated refresh tokens (defaults to this .env file).
# NOTE: it cannot be set in this file — the server resolves it BEFORE reading
# .env. Set it in the host process env instead (e.g. the "env" block of your
# MCP server config). See README "Read-only or containerized installs".

# Restriction Mode — omit or set to false to register all tools (default: all tools registered)
# QUICKBOOKS_DISABLE_WRITE=true    # suppress create_* and create-* tools
# QUICKBOOKS_DISABLE_UPDATE=true   # suppress update_* and update-* tools
# QUICKBOOKS_DISABLE_DELETE=true   # suppress delete_* and delete-* tools
